export enum notificationStatus {
  noInv = "noInvitation",
  invPending = "invitationPending",
  invAccepted = "invitationAccepted",
}

export enum notificationType {
  proReq = "projectRequest",
  jobReq = "jobRequest",
  parRequ = "partnerRequest",
  punctuation = "punctuation",
}
